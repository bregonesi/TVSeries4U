<% provide(:title, "All actors") %>

<% if logged_in && current_user.admin %>
  <div>
    <%= link_to "Add Actor", new_actor_path, class: "btn btn-success" %>
  </div>
<% end %>

<h1>All actors</h1>
<%= render "index" %>
