<% provide(:title, "All genres") %>

<% if logged_in && current_user.admin %>
  <div>
    <%= link_to "Add Genre", new_genre_path, class: "btn btn-success" %>
  </div>
<% end %>

<h1>All genres</h1>
<%= render "index" %>
