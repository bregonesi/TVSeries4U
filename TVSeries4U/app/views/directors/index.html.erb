<% provide(:title, "All directors") %>

<% if logged_in && current_user.admin %>
  <div>
    <%= link_to "Add Director", new_director_path, class: "btn btn-success" %>
  </div>
<% end %>

<h1>All directors</h1>
<%= render "index" %>
